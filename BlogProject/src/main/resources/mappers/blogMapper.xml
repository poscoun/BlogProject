<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.gblog.dao.BlogDAO">
	
<!-- 	<insert id="bloginsert">
		insert into blog (user_id)
		select #{user_id} from DUAL
		where not exists (select user_id from blog where user_id = #{user_id})
	</insert> -->
	
<!-- 	<select id="blogselect" resultType="int">
		select blog_id
		from blog
		where user_id = #{user_id}
	</select> -->
	
<!-- 	<select id="bloglist" resultType="com.gblog.dto.BlogDTO">
		select *
		from blog
	</select> -->
	
</mapper>